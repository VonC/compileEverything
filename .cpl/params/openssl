configcmd=./config --openssldir=${HULS}/@@NAMEVER@@ zlib shared --with-zlib-include=${HULI} --with-zlib-lib=${HULL}
linkdst=${HUL}/ssl
post=rm -f ${HULS}/@@NAMEVER@@/include/openssl/include ; ln -fs ../openssl ${HULS}/@@NAMEVER@@/include/openssl/include
