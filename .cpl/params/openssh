configcmd=./configure --prefix=${HULS}/@@NAMEVER@@ --enable-shared --enable-static --with-zlib=${HUL} --with-ssl-engine=${HUL}/ssl --without-privsep-user --with-pid-dir=${HUL}/var/run --with-default-path=@@PATH@@ --with-privsep-path=${HUL}/var/empty @@WITHOUT_GNU_LD@@
post=ln -fs ../../../.ssh/cnf "${H}/usr/local/etc/sshd_config" ; cp_tpl "${H}/.ssh/cnf.tpl" "${H}/.ssh" ; mkdir -p "${H}/usr/local/var/run" ; cp_tpl "${H}/.ssh/config.tpl" "${H}/.ssh" ; ln -fs ../../../.ssh/config "${H}/usr/local/etc/ssh_config"; chmod 700 "${H}/.ssh"
